#!/usr/bin/env fish

# set -gx XDG_DATA_HOME "$HOME/.local/share"
# set -gx XDG_CONFIG_HOME "$HOME/.config"
# set -gx XDG_DATA_HOME "$HOME/.local/state"
# set -gx XDG_CACHE_HOME "$HOME/.cache"

set -g fish_key_bindings fish_vi_key_bindings 

# editors
set -gx VISUAL "nvim"
set -gx EDITOR "nvim"
